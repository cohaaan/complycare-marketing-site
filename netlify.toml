# Netlify build + SPA (client-side routes). Also see public/_redirects and public/_headers.
[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  publish = "dist"
